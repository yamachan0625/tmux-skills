[core]
mode = "local" # local | github
manager_pane = "%0"
worker_panes = ["%1", "%2", "%3"]
poll_interval_sec = 2
max_attempts = 3
retry_backoff_sec = [30, 120]
worker_hard_timeout_sec = 3600
worker_idle_timeout_sec = 600
run_deadline_sec = 14400
timeout_retriable = true
output_language = "ja"
# output_language_instruction = "回答は日本語で、箇条書き中心で簡潔に"

[codex.manager]
ask_for_approval = "never"
sandbox = "danger-full-access"
profile = ""

[codex.worker]
ask_for_approval = "never"
sandbox = "danger-full-access"
profile = ""
model = "gpt-5.3-codex"
json_output = true

[local]
queue_dir = ".agent/tasks"
result_dir = ".agent/results"
done_dir = ".agent/done"
failed_dir = ".agent/failed"

[github]
repo = "owner/repo"
project_url = "" # optional: https://github.com/users/<login>/projects/<number> or /orgs/<org>/projects/<number>
workflow = "issue-only" # issue-only | pr-loop
queue_label = "agent:queued"
running_label = "agent:running"
review_label = "agent:review"
done_label = "agent:done"
failed_label = "agent:failed"
needs_human_label = "agent:needs-human"
scope_label = ""
close_on_done = true
comment_post_max_attempts = 2
verify_comment_after_post = true
pr_merge_method = "squash" # merge | squash | rebase
pr_delete_branch = true
pr_require_checks = true
review_retry_backoff_sec = 60
review_wait_max_cycles = 20
dag_label = "agent:dag"
dag_blocked_label = "agent:blocked"
root_label = "agent:root"

interface:
  display_name: "Review"
  short_description: "Run code review checks and report findings."
  default_prompt: "Use $review to inspect current code changes and report issues by severity."

dependencies:
  tools:
    - type: "command"
      value: "git"
      description: "Git CLI used for status, diff, and history inspection."
      command: "git"

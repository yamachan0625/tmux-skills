interface:
  display_name: "tmux sender"
  short_description: "Send commands safely to another tmux pane."
  default_prompt: "Use $tmux-sender to send a command to a target pane_id and capture output."

dependencies:
  tools:
    - type: "command"
      value: "tmux"
      description: "tmux CLI used to send commands and capture pane output."
      command: "tmux"
